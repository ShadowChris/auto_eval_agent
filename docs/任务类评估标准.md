# 任务类（录屏）评估标准

> 最后同步：2026-08-05
>
> 唯一规则源：`config/skills/operation.yaml`；如与本文不一致，以 YAML 配置为准。
>
> 输出字段：`correctness`、`issue_types`、`rationale`、`is_low_level`

## 1. 核心原则

1. 基于整个 query 判断，不将复杂任务拆成多个独立 case。
2. 先识别实际生效目标、目标依赖关系和条件分支，再判断是否闭环。
3. 只要存在影响结果的可归责错误，整体优先判为 `nok`。
4. 没有可归责错误，但受到无法自主补齐的条件阻塞时，判为 `no_support`。
5. 临时环境异常、评测输入异常或未预期场景，判为 `others`。
6. `issue_types[0]` 必须是决定 correctness 的主要根因；后续元素只记录其他独立问题或通用质量问题。
7. 区分“幂等状态设置”和“依赖活动对象的动作”：前者当前状态已满足可判 `ok`，后者缺少正在运行的对象时判 `no_support`。

## 2. Correctness 定义

| Correctness | 中文含义 | 判定条件 | 典型场景 |
|---|---|---|---|
| `ok` | 任务正确闭环 | 简单任务唯一生效目标完成，或复杂任务所有生效目标完成；无需用户继续操作；条件任务执行正确分支；幂等状态设置的初始状态已满足也算完成。轻微过程问题只记录 issue。 | 设置卡片显示目标开关已生效；查询结果已通过文字或卡片交付；条件不成立时正确跳过后续动作。 |
| `nok` | 存在可归责执行错误 | 任一必要目标未执行、结果错误、执行中断、第三方跳转中断、执行后没有可验证结果，或 agent 回复与界面明确冲突。复杂任务中任一目标有错，整体优先判 `nok`。 | 停在本可继续完成的步骤；多任务只完成部分目标；设置了错误值；没有用户操作指引而静默停在登录页或设置页；声称完成但没有结果状态。 |
| `no_support` | 受到客观条件阻塞 | 不存在影响结果的可归责错误，但缺少 agent 无法自主补齐的条件。等待用户场景需要界面、任务执行过程文字或最终回答明确提示用户登录、授权、验证、补充、选择或确认；明确说明缺少完成任务所需的信息并指引用户提供，或声明能力不支持且无可信反证时，也属于 `no_support`。 | 权限弹窗等待用户授权；预订前给出候选项并询问用户选择；缺少待处理图片或文本；手机没有 SIM 卡；应用未安装；agent 明确说明当前设备不支持。 |
| `others` | 其他异常或无法可靠归类 | 不存在优先级更高的可归责错误，且因临时环境、算力、外部服务、评测输入或未预期场景无法完成或判断。 | 明确提示算力不足或系统繁忙；视频损坏；任务窗口始终未展开；直接证据互相冲突。 |

### 判定优先顺序

```text
存在可归责执行错误？ ── 是 ──> nok
          │
          否
          ↓
所有生效目标完成或正确跳过？ ── 是 ──> ok
          │
          否
          ↓
受到无法自主补齐的前置条件阻塞？ ── 是 ──> no_support
          │
          否
          ↓
临时系统、评测输入或未预期异常 ──> others
```

在进入上述判定前，裁判必须先完成阻塞证据门控：逐帧逐字摘录登录、授权、同意、验证、补充、选择或确认等用户指引，再逐字摘录 agent 文本中“无法、缺少、未提供、请提供、请选择、是否、要不要、请确认”等继续任务所需的信息，并区分界面指引、系统流程状态和 agent 自然语言回复。只有能够逐字引用、直接要求或询问用户下一步的完整句子或协议提示才算明确指引；普通“登录”入口、“查看”按钮、“手动操作中”状态、接管或停止控件、加载动画、“已完成”和“超时”都不算。不得通过图标、模式名称或页面类型推测指引。任一路出现明确指引且此前没有独立错误时，未完成、无结果、超时和返回其他界面只能作为阻塞后果；两路都无指引且静默停止时才判执行中断。

复杂多任务的阻塞只覆盖依赖该次用户答复的目标，不能自动豁免其他独立目标。其他目标已经出错、声称完成但没有证据，或者本可独立执行却完全未执行时，整体仍判 `nok`。任务采用串行执行本身不能证明某个登录或确认阻塞必然阻止全部后续目标。

## 3. Issue Type 定义

### 3.1 通用质量问题

这些类型可以和任意 correctness 搭配，默认不单独改变 correctness。

| Issue Type | 允许的 Correctness | 定义 |
|---|---|---|
| 内部过程信息泄露 | 全部 | agent 回复暴露内部推理、检索过程、skill、工具名称或调用过程，例如“我先想想任务规划”“加载某技能”。 |
| 回复语义重复 | 全部 | agent 回复前后重复表达相同含义，影响简洁性或阅读体验。 |
| 回复内容自相矛盾 | 全部 | agent 回复内部存在相互冲突的描述；只有影响最终结果理解或内容交付时才应判 `nok`。 |
| 重复系统卡片 | 全部 | 同一任务出现内容相同或高度重复的系统结果卡；只有造成错误结果或副作用时才影响 correctness。 |

### 3.2 `ok` 的轻微问题

| Issue Type | 定义 |
|---|---|
| 重复操作 | 存在不影响最终结果的重复点击或重复执行。 |
| 路径冗余 | 操作路径存在绕路，但最终结果正确闭环。 |
| 轻微无关步骤 | 存在少量无关步骤，但不影响最终结果。 |
| 其他轻微问题 | 任务正确闭环，但存在其他不影响 correctness 的轻微问题。 |

### 3.3 `nok` 的执行问题

| Issue Type | 定义与边界 |
|---|---|
| 应执行目标未执行 | Query 直接要求或条件成立后已生效、具备执行条件的目标没有开始执行。目标因条件未触发、明确缺少前置条件，或界面、最终回答已经明确等待用户操作时不使用。 |
| 任务结果错误 | 已经执行或交付结果，但最终结果与 query、可见证据或已知条件不一致。不得因为无法外部核验文字事实而使用。 |
| 执行中断 | 没有明确临时系统异常，界面和最终回答也没有用户操作指引，普通任务执行过程停止、卡住、静默超时或异常循环未结束。 |
| 三方应用跳转中断 | 跳转第三方应用后停在登录页、首页或其他中间状态，且界面和最终回答均没有明确用户操作指引。登录、授权或确认界面存在明确指引时改按“缺少前置条件”判断。 |
| 未展示可验证结果 | 有证据表明目标已尝试，或 agent 明确声称完成，但画面没有展示可验证结果。目标完全未执行时改用“应执行目标未执行”；目标已经明确等待用户操作时，缺少最终结果只是阻塞后果，不使用本类型。 |
| 回复与界面不一致 | agent 面向用户的自然语言回复与录屏中的可见状态明确冲突。系统任务面板中的步骤文案、勾选、“√已完成”“超时，任务终止”等流程状态不属于 agent 回复；必须指出发生冲突的具体自然语言回复。 |
| 其他执行问题 | 已确认存在影响结果的可归责错误，但现有 `nok` 类型无法覆盖。 |

### 3.4 `no_support` 的阻塞问题

这些类型在复杂任务整体因其他目标错误判为 `nok` 时，也可以作为次要 issue。

| Issue Type | 定义与示例 |
|---|---|
| 待用户澄清 | 为继续完成原始任务，界面、任务执行过程文字或 agent 最终回答已发起明确询问并等待补充、选择或确认。前置查询、搜索、导航到目标页或内容准备已经完成，不代表要求实际动作的原始任务已经闭环。 |
| 缺少前置条件 | 缺少图片、待处理文本或内容、应用、登录、权限、身份验证、目标对象、硬件、设备能力或助手能力等无法自主补齐的条件。明确说明缺少必要信息并指引用户提供时属于本类型。可信反证仅限配置先验或当前可见的同一目标成功证据，不能使用泛化能力常识、相似能力或设想的替代策略反驳。 |
| 其他阻塞原因 | 已确认没有可归责执行错误并受到客观阻塞，但不属于待用户澄清或缺少前置条件。 |

### 3.5 `others` 的异常问题

| Issue Type | 定义 |
|---|---|
| 环境或算力异常 | agent 明确说明算力不足、系统繁忙、遇到临时问题或需要稍后重试。 |
| 外部服务暂时异常 | 外部服务临时不可用，重试后可能恢复；稳定的能力不支持应归入“缺少前置条件”。 |
| 视频损坏 | 输入视频内容明确损坏，无法用于评测。 |
| 录屏数据不完整 | 输入的录屏数据本身明确不完整；不能仅因关键帧没有某状态而推测录屏不完整。 |
| 视频无法解析 | 输入视频无法被系统正常解析。 |
| 任务意图不明确 | Query 无法确定具体任务意图。 |
| 任务要求矛盾 | Query 内部要求互相矛盾，无法确定可执行目标。 |
| 输入数据异常 | Query、上下文、视频路径或其他评测输入存在异常。 |
| 任务执行窗口未展开 | 任务窗口始终处于缩略状态，过程和结果均不可见，且没有其他证据可供判断。 |
| 评测证据冲突 | 多项直接证据互相冲突，无法可靠确定最终状态。 |
| 未预期场景 | 出现现有 correctness 和 issue type 未覆盖的新场景。 |
| 其他未归类情况 | 已确认应判 `others`，但现有类型无法准确覆盖。 |

## 4. 证据规则

- 卡片、按钮、设置页面、系统状态、任务执行过程和结果页面都可以作为完成证据，关键是是否展示了具体目标状态。
- 设置结果卡直接显示目标开关状态或当前值时，可以作为充分完成证据，无需再次进入完整设置页面。
- 复杂任务的每个生效目标都必须有对应证据，一个目标的证据不能替代其他目标。
- 只有“正在操作”“已完成”、工具名称、入口页面或 agent 文字声称，不能证明任务完成。
- 不要求展示状态变化前后对比；幂等状态设置的最终状态或初始状态直接满足 query 即可。
- 查询、天气、攻略、推荐、作文和摘要等文字结果不主动进行外部事实核验，但仍需检查是否完成 query、是否遗漏内容，以及是否与录屏、上下文或可信先验冲突。
- 必须按时间顺序同时检查视觉证据和完整 agent 文本自述，再把两路证据联合判断；不得只看其中一路。明确的用户操作指引可以出现在弹窗、任务执行过程文字或最终回答中，不要求两路证据重复出现同一句话。
- 对未完成目标先判断最后有效状态。界面、任务执行过程文字或最终回答有明确登录、授权、验证、补充、选择或确认指引时属于等待用户；没有任何用户操作指引、最终回答为空且流程停止或超时属于静默停滞。登录、授权、协议同意和身份验证等需要用户本人确认或输入的界面，只要自身包含明确操作指引即可证明等待用户，不要求 agent 代替用户点击或输入。agent 已无错误地完成前置步骤并发起与原始任务闭环直接相关的明确询问时，按实际等待状态判断；不能因已经展示部分结果而提前判 `ok`，也不评价是否存在其他更优的免询问策略。超时或终止本身不是等待用户的证据，后续返回桌面或助手界面也不能覆盖此前的明确等待状态。目标已经明确等待用户时，不得再因用户未响应造成的未执行、中断或无最终结果改判为执行错误。
- 先确认 query 的原始完成边界。query 本身要求查询、搜索、推荐或列举时，文字或卡片结果可以完成任务；query 要求预订、下单、发送或发布等实际动作时，前置查询和候选列表只是执行过程，随后询问用户选择、补充或确认应判 `no_support / 待用户澄清`，不能按查询任务提前判 `ok`。

### 4.1 初始状态与动作前置条件

| 指令类型 | 示例 | 当前已经处于目标状态时 |
|---|---|---|
| 幂等状态设置 | 关闭定位、开启蓝牙、设为静音 | 可以直接判 `ok`，因为用户要求的目标状态已经满足。 |
| 依赖活动对象的动作 | 停止播放、暂停下载、挂断电话、取消导航 | 如果没有正在运行的对象，判 `no_support / 缺少前置条件`；不能仅因最终呈现为“未播放、未通话、未导航”而判 `ok`。 |

如果活动对象存在但 agent 没有执行动作，判 `nok`；如果活动对象不存在且 agent 正确说明情况，判 `no_support`。

## 5. 评测先验知识

| 场景 | 统一解释 |
|---|---|
| SIM 卡 | 当前任务类评测手机默认未安装 SIM 卡；涉及电话、短信或移动数据时，SIM 缺失是可信设备条件。 |
| “√已完成” | 只表示任务执行流程或当前步骤结束，不代表用户目标已经完成，也不能单独作为完成证据、agent 回复或回复冲突证据。同一任务面板出现明确确认或选择指引时，应判为等待用户；后续超时不覆盖该根因。 |
| 播放结果 | 搜索结果卡、影视详情卡、播放按钮或流程“已完成”只能证明找到内容或准备播放。要求播放时，必须看到播放器、播放进度、暂停按钮或其他直接播放状态；只有结果卡时判 `nok / 未展示可验证结果`。 |
| 播客生成 | 出现可识别的播客结果卡即视为播客生成目标完成，即使卡片仍显示“生成中”或尚未自动播放。 |
| 第三方登录页 | 登录、授权或身份验证页面可以作为阻塞证据，但还需要界面自身或 agent 最终回答出现明确用户操作指引；只有普通登录入口、回答为空并静默停住时按执行中断处理。 |
| 任务窗口缩略 | 窗口始终没有展开，导致过程和结果完全不可见时，判 `others / 任务执行窗口未展开`。 |
| 录屏工具控件 | 顶部红色计时、录制胶囊、触控提示等属于录屏工具，不是 agent 操作或任务完成证据。 |

## 6. 输出约束

```json
{
  "correctness": "ok|nok|no_support|others",
  "issue_types": ["受控中文问题类型"],
  "rationale": "逐个生效目标说明结果、证据或阻塞，并解释主判定",
  "is_low_level": "yes|no"
}
```

- `nok`、`no_support`、`others` 至少输出一个 issue type。
- `issue_types[0]` 是主要根因，禁止将通用质量问题放在非 `ok` 结果的第一位。
- 简单任务被判 `nok`，且意图清晰、没有客观阻塞并出现明显基础执行错误时，`is_low_level=yes`。
- `ok`、`no_support`、`others` 和复杂多任务固定 `is_low_level=no`。
